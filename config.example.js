// Copy this file to config.js and fill in the real value before deploying.
// config.js is gitignored so the curator password stays out of the repo.
window.ADA_CONFIG = {
  curatorPw: "CHANGE_ME"
};
